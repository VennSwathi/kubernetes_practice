---
apiVersion: v1
kind: Service
metadata:
  name: fb-service
  labels:
    name: fb-service
    app: fb-app
spec:
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  selector:
    app: fb-app
    env: prod
  
  